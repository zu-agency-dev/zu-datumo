{
  "name": "zu-datumo",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "pnpx esbuild --bundle src/counter.js --outdir=dist --minify --sourcemap --platform=node --format=esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "esbuild": "0.19.2",
    "google-spreadsheet": "^4.0.2"
  }
}
